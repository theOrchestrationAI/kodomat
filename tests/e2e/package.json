{
  "name": "slavkokernel-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for SlavkoKernel",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.36.0",
    "dotenv": "^16.3.1"
  }
}